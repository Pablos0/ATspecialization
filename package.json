{
  "name": "jsuitask",
  "version": "1.0.0",
  "description": "1. Walk through the provided materials (official documentation, video) to understand how WDIO works and the main benefits of the tool.\r 2. Create an initial setup of WDIO on the local machine\r 3. Create WDIO config if it does not exist and familiarize\r 4. Create first specs using the existing BDD scenario created in Module 2\r 5. Execute these tests using CLI in different browsers (Chrome, Firefox, Safari) in headless mode\r 6. Execute tests in parallel using 2 instances\r 7. Add the option to run tests 2 times before marking it as failed\r 8. Push the code to remote repository and create Merge Request",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "wdio": "wdio run wdio.conf.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "@wdio/cli": "^9.20.0",
    "@wdio/local-runner": "^9.20.0",
    "@wdio/mocha-framework": "^9.20.0",
    "@wdio/spec-reporter": "^9.20.0"
  }
}
